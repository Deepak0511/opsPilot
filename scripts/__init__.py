"""
scripts package
---------------
This package contains database seeder modules (`init_db.py`), the interactive 
CLI runner (`cli.py`), and graph visualization utilities (`visualize_graph.py`).

These tools help in bootstrapping the OpsPilot environment and testing the 
agent outside of the Streamlit UI.
"""
